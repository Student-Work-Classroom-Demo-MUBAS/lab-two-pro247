#Reflections
6. It will be blue this is so as the class has a higher priority compared to the element.
7. rem stands for Root EM, since rem is a relative unit which is tied to the root font-size which is controlled by the default browser's settings this respects the user's explicit preference rather than overriding it since px is an absolute unit. Since every rem value is recalculated against the new root value the value will increase as 20/16 = 1.25X.
8. As everytime the html loads, in the link tag a request is made to a separate network and when this fails to fetch html gives up and uses tha capability of font family to use the next font in the family that is inbuilt in css.
9. H - Hue 0 to 360 degrees this controls the xolour itself e.g 0 is red.
S - Saturation 0 to 100%, this i sthe itensity of the color e.g 0% is gray.
L - Lightness 0 to 100% this is how light or dark with 0% being black
To make hsl(120, 100%, 50%) much lighter the lighness has to be around 85 to 90%, for example it can now be hsl(120, 100%, 685%).
